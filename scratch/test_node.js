const fs = require('fs');
const { createCanvas, loadImage } = require('canvas');

// Alternatively, if canvas is not installed, we can write a simple node script using standard Jimp or C# / powershell inline C#
